package com.infosys.db.migration.hackathon.util;

public class SelectRuleFinderUtil {

	private SelectRuleFinderUtil(){
	}

	/*
	 * This will check the pattern of a SELECT statement and decide 
	 * which select pattern it fits into.
	 * 
	 * Based of select pattern corresponding MongoDB rule will be decided 
	 * which will later form in helping MongoDB statement
	 * 
	 */
	public String findRule(final String sqlStmt){
		String rule = "";
		
		
		return rule;
	}
	
}
